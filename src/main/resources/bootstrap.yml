server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JPerez11/service-configuration-pragma
          clone-on-start: true
          default-label: master

  security:
    user:
      name: root
      password: s3cr3t